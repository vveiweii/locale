<div class="card mb-4 border rounded shadow-sm">
  <div class="card-body">
    <div class="col-12">
      <div class="mb-3 text-center">
      </div>
      <div class="d-flex justify-content-start gap-2">
        <p><b><%= business.name %></b></p>
        <p class="small mt-2"><%= truncate(business.description, length: 50, separator: ' ', omission: '...') %></p>
        <p><%= link_to "Learn More", business_path(business) %></p>
      </div>
    </div>
  </div>
</div>
