<%= link_to business_path(business), class: "card-link" do %>
  <div class="card rounded">
      <% if business.photos.attached? %>
        <%= cl_image_tag business.photos.first.key, class: "card-img-top card-img", crop: :fill %>
      <% else %>
        <%= image_tag "business-placeholder.jpg", class: "card-img-top card-img", crop: :fill %>
      <% end %>
      <div class="card-body">
        <div>
          <%= @reviews_average_rating %>
          <% @reviews_average_rating.to_i.times do %>
            <p style="display: inline-block;"><i class="fa-solid fa-star fa-md" style="color: #ffea00;"></i></p>
          <% end %>
          <p><b><%= business.name %></b></p>
          <p><%= business.address %></p>
        </div>
      </div>
  </div>
<% end %>
